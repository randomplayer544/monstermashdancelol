local player = game.Players.LocalPlayer

local char = player.Character

local bd = Instance.new("BodyVelocity",char.HumanoidRootPart)

while task.wait() do
   bd.Velocity = game.Players.LocalPlayer:GetMouse().Hit.LookVector * 15
   bd.MaxTorque = Vector.create(math.huge,math.huge,math.huge)
end
